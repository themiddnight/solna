# SDD ledger — plan: docs/superpowers/plans/2026-09-06-drum-grid-genre-accuracy-part1.md

Spec: docs/superpowers/specs/2026-09-06-drum-grid-genre-accuracy-design.md (reachable, read).
Branch: refactor/data-layer-extraction. Preflight BASE: b905ed5.
10 tasks, 70 steps. Gate per task: `bun run verify` (eslint zero errors).

## Preflight conflict scan

### Cross-task pairs (shared file or interface)

| pair | shared surface | produces -> consumes | finding |
|---|---|---|---|
| T1 -> T3,T4,T5 | src/data/drumGrids.ts, drumGrids.test.ts | T1 produces `provenance: string` + AUTHORED_ALLOWLIST; T3 shrinks the allowlist, T4 adds 9 sourced entries, T5 removes one | clean; T1-first is stated and load-bearing (a URL must not precede the rows it claims) |
| T3 <-> T4 | drumGrids.ts / .test.ts | T3 rewrites 10 existing entries, T4 appends 9 new ones | clean, disjoint entry sets |
| T4 <-> T5 | meterRegression.ts FOUR_FOUR_GRID_IDS; drumGrids.test.ts counts | T4 +9 ids, T5 -1 id | clean; ordering rationale 3 keeps each diff readable |
| T5 <-> T6 | src/data/vibes.ts | T5 repoints cyber-edm to `house`; T6 authors the 8 `random.drumGrids` pools | clean IF T6's cyber-edm pool contains `house` (pool invariant: dice lands back on the vibe as authored). Order T5<T6 is correct. Carried into T6's dispatch. |
| T5 <-> T7 | src/store/instantVibesDrums.test.ts | T5 edits :73 (id list), T7 edits :40 (a comment) | clean, different lines |
| T6 <-> T7 | the drum apply path | T6 lands the grid-id pool while the store action still MERGES; T7 makes it replace | MEASURED harmless: merge-vs-replace is only observable on a row some track can play. All 22 grids declare exactly 7 rows in two variants (`...,tom,bass` and `...,tom,crash`); they differ only on bass/crash, and neither has a sequencer track until T8. Ruling below. |
| T7 <-> T9 | src/store/store.test.ts | T7 edits 242-280,411; T9 edits 883,918 | clean, different regions |
| T8 -> T9 | src/store/initialState.ts | T8 produces `withDrumTracks` + 2 tracks; T9 calls it from both chains | clean, and the colour-list question is resolved below |
| T5 <-> T10 | .claude/skills/instant-vibes/SKILL.md | T5 edits :176; T10 sweeps | clean |
| T3,T4,T5 -> golden fixture | src/store/instantVibesDrumsFixture.ts | T3 rewrites 10 grids; no vibe references any of them (measured: the 8 vibes point at the 8 vibe grids only) | clean; the single fixture move is cyber-edm gaining house's `bass` row, exactly as T5 states |

### Per-task self-agreement

| task | its tests vs its code, its files vs later edits | finding |
|---|---|---|
| T1 | provenance is a plain string field on a literal -> src/data purity holds; every grid marked 'authored' at that moment is true | agrees |
| T2 | pinned sha `00a61f1` verified to exist (`git cat-file -t` = commit); report is outside `verify` by design | agrees |
| T3 | the ten ids (techno, synthwave, dubstep, trap, boom-bap, lofi-hip-hop, funk, reggae, waltz, afro-6-8) are all sequencer genre grids; test names cite their source | agrees |
| T4 | nine new keys, no type change, third id group in the test file | agrees |
| T5 | `house` becomes the one 8-row grid; the plan pre-empts the "seven rows and nothing else" test (drumGrids.test.ts:102) with an explicit exception table rather than a loosened assertion | agrees |
| T6 | `current` stays 3 fields; `eligibleFor` kept, decoration deleted; no new store state (the departure from the brief is argued in the plan's ordering section and settled in the spec) | agrees |
| T7 | same signature, different contract; all call sites enumerated with line numbers | agrees |
| T8 | 5 -> 7 tracks at bg-primary/bg-info (both real THEME_TOKENS); `withDrumTracks` appends fresh object AND fresh steps array | agrees |
| T9 | two chains, two symbols, two version bumps; never merged | agrees |
| T10 | last, so it records final counts | agrees |

### Rulings made at preflight

- Ruling: keep the T6-before-T7 order rather than swapping to close the one-commit merge-semantics window — measured, the window is unobservable, because the only rows the two grid row-sets disagree on (`bass`, `crash`) have no sequencer track until T8, and T7 lands before T8. Cost if wrong: one commit on a feature branch in which a dice reroll onto a differently-shaped grid could leave a stale row; no release happens between the two commits.
- Ruling: leave store.test.ts:883,918 assigned to T9, not T8 — verified by reading them, both payloads supply their own short `sequencerTracks` array, so rehydration returns exactly what the payload held until T9's `migrateDrumTracks` appends the missing canonical tracks (each list then becomes seven). T8's gate stays green with the lists untouched. Cost if wrong: T8's `bun run verify` goes red on two colour assertions and the implementer fixes them a task early.

Task 1: complete (commits b905ed5..7bb4d54, review clean — spec ✅, quality approved, no findings)
Task 2: review found 2 Critical, both plan-mandated (the brief's own template code carries them).
Task 2: Ruling: fix both rather than park — they do not contradict the plan, they enforce it. The plan's own Interfaces block for Task 2 states "stdout only, **exit code always 0**", and a reachable uncaught throw around `await import(...)` breaks exactly that contract; the leaked `mkdtempSync` directory (reviewer reproduced it: 6 stale dirs in $TMPDIR) is a side effect a report has no business leaving behind. Cost if wrong: a few lines of try/finally in a script that is outside the gate and asserts nothing.
Task 2: minor (deferred): scripts/report-drum-diff.ts strips baseline imports with a literal `line.startsWith('import ')`, so a future multi-line import in a bumped BASELINE would leave broken source. Neutralised in practice once the import is wrapped in try/catch — it becomes a reported failure instead of a crash. Pinned sha 00a61f1 has a single-line import (verified).
Task 2: fix round 1/5 (2 addressed, 0 open; commits c32b95d..0914394)
Task 2: complete (commits 7bb4d54..0914394, review clean)

Task 3: implementer returned DONE_WITH_CONCERNS, no commit — the sourced corrections created silent duplicates the plan did not anticipate. It stopped and reported rather than editing around it, which is what the dispatch asked for.
Task 3: controller measurement (bun -e over DRUM_GRIDS, all pairs, playable rows kick/snare/hihat/openhat/clap):
  - house == edm-offbeat-pump   (pre-existing; Task 5 deletes one)
  - techno == synthwave-four-on-floor   (NEW, reported by the implementer)
  - rock == lofi-hip-hop   (NEW, NOT reported — genre x genre, and the existing sweep only compares vibe x genre, so no test can express it)
  - afro-six-eight-bell == afro-6-8 has dissolved, as the brief predicted.
  Kits, measured: house/edm-offbeat-pump are BOTH '909 Modern'; techno is 'Warehouse' vs synthwave-four-on-floor 'Retro Drive'; rock is 'Acoustic Studio' vs lofi-hip-hop 'Lo-Fi Vinyl'.
  Picking a grid applies BOTH its rows and its kit — `SequencerView.tsx:63-66` runs `onChangeSoundKit(DRUM_GRIDS[selectedGridId].kit)` on every selection, and the file's own comment says "A grid names the kit it was written for, so picking one loads both."
Task 3: Ruling: widen the silent-duplicate definition to same meter + same playable rows + SAME KIT, and widen the sweep from vibe-x-genre to all pairs — because a "silent duplicate" is supposed to mean "these two sound the same", and measurably they do not: two entries with identical step positions and different kits produce different sound the moment either is picked. The sweep must cover genre-x-genre too, or spec item 21's claim that an empty allowlist leaves "no allowlist to hide behind" is false for half the library. Under the widened definition the only surviving pair after Task 3 is edm-offbeat-pump/house (both '909 Modern'), so Task 5 still takes the array to `[]` and spec item 21's end state is unchanged. Cost if wrong: if a later change stops the grid picker applying the grid's kit, two kit-distinguished entries become real duplicates with no test catching them — mitigated by writing that dependency into the test's own comment.
Task 3: Ruling: Task 3 also removes ['afro-six-eight-bell','afro-6-8'] from SILENT_DUPLICATES, overriding the brief's "do not touch SILENT_DUPLICATES here; Task 5 empties it in one move". Following the brief literally would end Task 3 with `bun run verify` red, and no task may leave the gate red. It also contradicts this plan's own provenance principle — a claim moves in the same commit that makes it untrue. Task 5 still removes the last pair and empties the array. Cost if wrong: the "one move" reads as two commits in the history instead of one.
Task 3: reviewer independently re-measured all 231 unordered pairs and all ten grids' step indices against the brief — no transcription slip, exactly ten entries changed, name/meter/kit unmoved on all ten, both rulings correctly applied and ruling 1's premise confirmed true.
Task 3: minor (deferred): drumGrids.test.ts:210 — the afro pair was removed from SILENT_DUPLICATES but not moved into GENUINELY_DIFFERENT (:168-171), so it now rests only on the exhaustive sweep's negative assertion and lost its named, reasoned per-pair test. One line closes it.
Task 3: minor (deferred): drumGrids.test.ts:99 — the 'rows survived the merge byte-for-byte' test had its synthwave.rows.kick expectation updated, which the brief's Files header did not list. Unavoidable (that test pinned the old value of a grid this task corrects) and the new comment names Task 3 as the reason.
Task 3: complete (commits 0914394..3451afa, review clean — spec ✅, quality approved, 2 minors deferred)
Task 4: reviewer re-measured all nine grids' step indices row by row and swept all unordered pairs over 31 grids — zero transcription slips, all nine kits exist in DRUM_KITS, all nine provenance strings byte-identical to the brief, 154 insertions / 0 deletions (purely additive), FOUR_FOUR_GRID_IDS order verified element-for-element. Collision sweep matched expectation exactly: full = edm-offbeat-pump/house only; weak = techno/synthwave-four-on-floor and rock/lofi-hip-hop only.
Task 4: the implementer deviated from brief Step 4 (which would have rewritten the sweep back to a vibe-x-genre loop and silently NARROWED Task 3's widened predicate) and instead extended ALL_IDS. Reviewer confirmed this is the correct call and the report names the deviation. No ruling needed — the brief was written before Task 3's ruling existed.
Task 4: minor (deferred): drumGrids.test.ts:319 and :259 — comments still say "22 ids" / "22 entries" where the sweep now covers 31. A later reader trusts a stale count when deciding whether the sweep covers a new group.
Task 4: minor (deferred): nothing pins tom/bass all-false on the nine new grids — drumGrids.test.ts:132-141 asserts only the row KEYS. A later edit could invent a tom fill on a sourced grid with no test failing, which is the exact class of drift this change exists to stop.
Task 4: minor (deferred): each of the nine shares its provenance URL with the genre entry corrected in Task 3 (one article, two patterns). Legitimate, but nothing records that as intended, so a future duplicate-provenance check would read it as an error.
Task 4: complete (commits 3451afa..46e355d, review clean — spec ✅, quality approved, 3 minors deferred)
Task 5: reviewer verified by measurement that house's kick/snare/hihat/openhat/clap/bass are byte-identical to 46e355d, that tom/crash carry edm-offbeat-pump's exact old values into what was an all-false tom and an absent crash (additive, nothing overwritten), and that the other 29 grids did not move a single byte. Golden fixture diff = `cyber-edm.bass:ADDED` and nothing else. Widened sweep intact (not narrowed back), SILENT_DUPLICATES = [] with the loop kept, row-set handled by an EXTRA_ROWS exception table rather than a loosened assertion. All three of the implementer's beyond-brief edits judged necessary and smallest-correct.
Task 5: minor (deferred): instantVibesDrums.test.ts:38-44 — the EXTRA_ROWS exception widens the key-set assertion but the length/cell-type loop still iterates only ROWS, so cyber-edm's `bass` row is never checked for length or boolean cells (measured correct today: 16 boolean). Fix is `[...ROWS, ...(EXTRA_ROWS[id] ?? [])]`.
Task 5: minor (deferred): drumGrids.test.ts:266-274 — the describe-block comment still says two vibe grids play identically to a genre grid "which is why they are still separate entries", which this task just made false and which contradicts `SILENT_DUPLICATES = []` thirty lines below it.
Task 5: Ruling: the stale-comment minors in src/data/drumGrids.test.ts have now accumulated across Tasks 3, 4 and 5 (three ledger entries: "22 ids"/"22 entries" counts at :267 and :328, the false describe-block comment at :266-274, and the afro pair losing its named per-pair test). Rather than leave them to the final review, I am extending Task 10's docs sweep to cover the comments inside src/data/drumGrids.test.ts as well as the head comments the plan names. Reason: Task 10 exists to make the documentation tell the truth about the final counts, and a test file's comments are documentation a reviewer reads first. Cost if wrong: Task 10's diff is a few lines wider than the plan specified, in a file it was already going to be measured against.
Task 5: complete (commits 46e355d..4d6881d, review clean — spec ✅, quality approved, 2 minors deferred + 1 pre-existing)

Task 6: controller verification of the implementer's concern 1, by evaluation. All 8 pools: every id exists in DRUM_GRIDS, and every vibe's own drumGridId is in its own pool (the "dice lands back on the vibe as authored" invariant holds, including cyber-edm -> house after Task 5's repoint). Cross-meter members by MeterId comparison = 4, not the brief's 2: lofi-waltz/afro-6-8 (3/4 -> 6/8) and afro-six-eight/waltz (6/8 -> 3/4) are the same 12 steps and differ only in accent grouping; lofi-waltz/lofi-ghost-kick and afro-six-eight/reggae-rockers (both 4/4 -> 12 steps) are real trims.
Task 6: Ruling: accept the implementer's fix — keep the authored pools verbatim and widen the test's expected list to the four measured entries, with the comment separating accent-grouping members from real trim cases. The brief's prose was reasoning in step counts (3/4 and 6/8 are both 12) while the test it specified compares MeterId, so the brief's "2" was counting only the trims; the pools are correct and the test was wrong. Cross-meter pooling is an explicit user decision, so the 3/4<->6/8 pairings are intended, not accidents. Cost if wrong: a test that names four cross-meter pool members where a stricter reading would name two, in a design where cross-meter pooling is deliberately allowed.
Task 6: Ruling: fold the `bun run report:library` gap into Task 10. `scripts/report-library-coverage.ts:44-47` folds the other three random pools into its coverage count but has no line for `r.drumGrids`, so it now under-reports drum-grid usage (says 8/30 referenced when the pools reference many more). This is a reporting tool whose printed count is now false, which is exactly what Task 10 exists to fix, and the instant-vibes skill tells authors to run it to find unreferenced entries. Cost if wrong: a one-line addition lands in the docs-sweep commit rather than its own.
Task 6: reviewer independently instrumented the draw counter (exactly 6 draws; a 5-entry script throws, a 6-entry one does not — no off-by-one in the shortened fixtures), confirmed all 12 banned decoration symbols return 0 hits across src/ and scripts/, `eligibleFor` kept and still used by pickDistinct, `current` unchanged at three fields, `activeDrumGridId` 0 hits, no runtime import added to src/data/vibes.ts, and drumGridName resolving to the display name. Cross-meter ruling implemented as ruled and its comment genuinely distinguishes the two classes.
Task 6: minor (deferred): vibeVariation.test.ts:370,384 both use `firstDraw`, which picks pool index 0 — the vibe's own grid — so neither test fails if the repoint is deleted. The real pinning lives in the enumeration test (:335) and the reachability test (:390), so nothing is unpinned today; `scriptedDraw([0,0,0,0,0,1])` at :370 would make them independently load-bearing.
Task 6: minor (deferred): nothing asserts the throw on an unknown grid id (vibeVariation.ts:97). Unreachable while pool invariant 2 holds — dead-but-cheap defence, not a gap.
Task 6: complete (commits 4d6881d..42ab8b1, review clean — spec ✅, quality approved, 4 minors deferred, 2 of which are already assigned to Task 10 by the ruling above)
Task 7: implementer extended the Task 10 stale-count list with src/data/drumGrids.ts:19 and drumGrids.test.ts:271 ("22 entries and not 14", now 30), plus CLAUDE.md still naming applyDrumPattern. All fall under the Task 5 ruling that widened Task 10's sweep.
Task 7: reviewer independently drove createSequencerSlice with a fake `set` and confirmed the contract by measurement — an unnamed track's window 0..15 goes false while padding at idx16/idx20 survives and steps.length stays 24; at 3/4 (window 12) idx13 and idx20 survive; a named track gets the adapted row; LOOP 12->16 and TRIM 16->12 both unchanged. applyVibeToStore not restructured (2-line diff, hardStopAll -> stopSource -> writes order intact). No live `applyDrumPattern` reference survives.
Task 7: the implementer's report OVERSTATED its mutation evidence — it claimed all 3 new tests go red when writeStepWindow is bypassed; the reviewer reproduced 2 of 3. Recorded because an implementer report that overstates evidence is a trust signal for the final review, not because the code is wrong: mutant A (restoring the merge) does turn all 3 red, and the bypass is still caught by store.test.ts:323.
Task 7: minor (deferred): store.test.ts:352 asserts `track.steps.slice(16)` equals `before[i].steps.slice(16)` where `before` is captured from state a previous test left behind — if the action ever truncates the array both sides become `[]` and the assertion passes vacuously. That is exactly why mutant B survived this test. One extra line asserting `track.steps.length` closes it.
Task 7: minor (deferred): every test in store.test.ts:239-379 runs at 4/4 (window 16 / storage 24). The reviewer verified 3/4 correct by measurement, but nothing in the suite pins it — so a future change from `stepsPerBar` to `MAX_STEPS_PER_BAR` in the clear path would stay green at 4/4 while silently destroying user programming in every narrower meter.
Task 7: minor (deferred): sequencerSlice.ts:44 `new Array(stepsPerBar).fill(false)` is typed `any[]` and so enters writeStepWindow unchecked; `new Array<boolean>(...)` closes it. tsc passes today.
Task 7: Ruling: the two test-strength gaps above (the vacuous slice assertion and the missing narrow-meter case) are fixed in Task 8's commit rather than left to the final review. Reason: they under-pin THIS task's own highest-risk constraint — that padding beyond the active window survives a clear — and Task 8 is the task that makes a cleared track audible by adding the tom and crash tracks, so a narrow-meter clear test belongs beside it. They are Minor by the review rubric and so did not enter Task 7's fix loop. Cost if wrong: two test additions land in the tracks commit instead of the semantics commit.
Task 7: complete (commits 42ab8b1..bbdcf68, review clean — spec ✅, quality approved, 5 minors deferred, 2 of them reassigned to Task 8 by the ruling above)

Task 8: controller verification by evaluation — 7 canonical tracks; tom=bg-primary, crash=bg-info; withDrumTracks(5)=7, idempotent (second call returns the same references), first five reference-identical (toBe holds), appended steps arrays are NOT the module constant's arrays, and tom/crash factory rows carry 0 hits.
Task 8: Ruling: accept the implementer's two deviations from the brief. The brief's test asserted that every appended track is silent while its own implementation copies factory rows; measured, that claim is false in general (kick 4 hits, snare 2, hihat 8, openhat 1, clap 2) and true only for the two tracks this task actually appends (tom 0, crash 0). The implementer kept the implementation — which the controller had pinned — and corrected the assertion and the same over-broad sentence in the JSDoc. Correcting a false assertion beats preserving a brief's wording. Cost if wrong: the test states a narrower guarantee than the brief's author intended, and the broader claim was measurably untrue anyway.
Task 8: the implementer corrected the controller's stated mechanism for the new 3/4 test: a `stepsPerBar` -> `MAX_STEPS_PER_BAR` mutation does NOT stay green at 4/4 (existing index-20 seeding catches it). The mutation that does survive at 4/4 is a clear width hardcoded to 16, and the new 3/4 test is the only test in the suite that catches it. The requirement was met; only my reasoning for it was wrong.
Task 8: the `steps.length` guard closes a real vacuity hole (the unknown-instruments test passed vacuously under a truncation mutation) but does not newly detect an otherwise-undetected mutation — three other tests already caught truncation. Reported honestly, including that a first measurement using `bun test -t` was invalid because the filter skips the test that seeds `before`.
Task 8: reviewer re-measured everything and mutation-tested four ways. withDrumTracks survives payloads that are already complete, empty, reordered, carrying an unknown instrument, or holding null/undefined/42 as elements — never drops or reorders. Both "simplified spread" mutations (appending `...missing` directly, and `{ ...t }` sharing the steps array) are caught. Confirmed the vacuity guard (3 fails without it, 4 with) and confirmed the new 3/4 test is the ONLY test in the file that catches a clear width hardcoded to 16. Re-measured the hit counts and agreed the corrected assertion is stronger than the brief's, not a tautology.
Task 8: minor (deferred): store.test.ts:337-368 — the new 3/4 test restores `setMeter('4/4')` at the end but outside a try/finally, and does not restore the sequencerTracks it filled true across all 24 cells. If an assertion throws mid-test the 3/4 meter leaks into the rest of the file and every later diagnosis is wrong.
Task 8: minor (deferred): initialState.test.ts:88-91 — the aliasing test checks only index 5 (`tom`); an implementation that copies tom's steps but appends crash by reference would pass. One line (`expect(a[6].steps).not.toBe(INITIAL_SEQUENCER_TRACKS[6].steps)`) closes it.
Task 8: minor (deferred): initialState.test.ts:110-115 — the loop runs over all 7 tracks including the two that came from the input by identity, so 2 of 7 iterations compare an object with itself. Stylistic; the guarantee is unaffected.
Task 8: minor (deferred): initialState.ts:39-43 — `SILENT_BAR` hardcodes 24 cells rather than deriving from MAX_STEPS_PER_BAR. Mitigated: the 'every track stores a full-width bar' test goes red immediately if that constant grows.
Task 8: complete (commits bbdcf68..fa27dfe, review clean — spec ✅, quality approved, 4 minors deferred)

Task 9: Ruling: the implementer's concern 2 — "a pre-v13 session where the user deleted every track gets the canonical seven back" — is not a regression, and the migration stands. Measured: the app has no delete-track or add-track affordance. `setSequencerTracks` is called from exactly two places (SequencerView.tsx:75 and :87, toggleStep and toggleMute), both of which `.map()` over the existing array and preserve its length. A user therefore cannot produce an empty sequencerTracks array; it can only come from a corrupt payload or sanitize's fallback. Restoring the canonical seven there is repair, not the silent overwriting of a user's intent. Cost if wrong: if a future version adds track deletion, this migration would resurrect deleted tracks once, and the fix would be to record deletions explicitly rather than infer them from absence.
Task 9: the implementer found FOUR pre-existing assertions went red, not the two the brief predicted (whose line numbers had drifted to :975 and :1010). The two extra are correct consequences: a `version: 1` payload asserting `sequencerTracks: []` now gets all seven appended, and the v8->v9 terminus test pinned `.version).toBe(12)`. Both updated without weakening.
Task 9: review found 3 Important (a docblock claim that is measurably false; a tautological assertion comparing two separately-declared functions; a vacuous idempotence test that runs no migration step) plus 3 Minor. Reviewer verified by running code that no two loops share a steps array, both chains are idempotent, an already-seven payload returns the same array reference, and neither chain imports the other. Entering fix round 1.
Task 9: minor (deferred): migrate.ts:377-379 and migrate.test.ts:502 — the top-level sequencerTracks branch is dead. partializeAppState (store.ts:156-171) has no top-level sequencerTracks and wrapFlatStateIntoLoop (migrate.ts:191) deletes every LOOP_FLAT_KEY before this step runs, so no payload the app has ever written reaches it and the test constructs one the app cannot produce. NOT a deviation — the brief mandated it verbatim as belt-and-braces with a comment saying so.
Task 9: minor (deferred): store.test.ts:821 — the re-point narrowed a full `toEqual([])` to a projection over `.instrument`; correct new value, honestly commented, but it no longer pins steps or colour.
Task 9: fix round 1/5 (3 addressed, 0 open; commits 1682a15..43cf883). Re-reviewer reproduced 2 of the 3 claimed mutations exactly and left the working tree clean.
Task 9: Ruling: accept the implementer's deviation on finding 2. My literal instruction (`expect(src).not.toContain('migrateDrumTracks')`) could not pass, because upgradeDrumTracksV5's docblock names the persist step in PROSE — symmetric with how upgradePadLayerV4's docblock names migratePadLayer. Deleting that cross-reference to satisfy a string match would have removed real documentation to make a test green. The substitute guards the actual rule instead of the word: `not.toContain("from './migrate'")` plus `not.toMatch(/migrateDrumTracks\s*\(/)`, verified red by adding a real call. Cost if wrong: the regex would miss a call split across a line break or made through an aliased import — though the import-line ban closes the path an alias would need.
Task 9: minor (deferred): the two-chain scan regex `/migrateDrumTracks\s*\(/` would not catch a call written across a line break, nor an aliased import calling it under another name. The companion `not.toContain("from './migrate'")` closes the most likely path.
Task 9: complete (commits fa27dfe..43cf883, review clean after one fix round — 3 Important addressed, 3 minors deferred)

Task 10: implementer found two more brief values wrong and wrote the measured truth: (a) "eight vibe grids ... one table of 30" sums to 31 — measured groups are 14 genre / 7 vibe / 9 variants, so seven; (b) "four of the five axes use pickDistinct" is three of five, because the drum axis uses plain draw.pick by design. Corrected report:library drum-grid line after the missing r.drumGrids fold: 21/30 referenced, 9 unreferenced (was 8/30).
Task 10: FLAGGED FOR REVIEW: `PLAYABLE` in drumGrids.test.ts is still the pre-Task-8 five rows (kick/snare/hihat/openhat/clap), but Task 8 made tom and crash playable. The silent-duplicate sweep would therefore miss a pair differing only on tom or crash. Green today; the implementer left it because fixing it is an assertion change and its scope was comments-only.
Task 10: reviewer re-derived every documented number from the real modules. 30 grids in 14 genre / 7 vibe / 9 variants; all carry provenance; house is the only 8-row grid; cyber-edm points at house; 7 tracks; 8 non-surface THEME_TOKENS with 7 used and only `neutral` free; all pools valid with 4 cross-meter members. Both of the brief's wrong numbers were corrected correctly. All three controller-ruling items landed, `bun run report:library` re-run by the reviewer gives `drum grids 21/30 referenced 9 unreferenced`, and the two data files were verified comments-only by filtering comment lines out of the diff and finding zero remaining lines.
Task 10: minor (deferred): drumGrids.test.ts:271 PLAYABLE is stale (5 pre-Task-8 rows) but NOT wrong today — the reviewer swept all 30 grids and found ZERO pairs sharing meter+kit and all five playable rows, so the sweep still detects correctly. Should be widened to the 7 playable voices BEFORE any new grid is authored, not after.
Task 10: minor (deferred): instantVibesDrumsFixture.ts:5 still says it pins "all eight vibes' seven rows" while cyber-edm has eight, and now contradicts SKILL.md:200 describing the same file.
Task 10: minor (deferred): CLAUDE.md:102-104 says "all five reroll axes are id pools" — `keys` are note names, not library ids, and `random` has six fields (bpm is a range), which reads as contradicting SKILL.md:153's "pins all six". Suggested wording: "five pick axes plus a bpm range".
Task 10: complete (commits 43cf883..2dac1a0, review clean — spec ✅, quality approved, 3 minors deferred)

ALL 10 TASKS COMPLETE. Branch: refactor/data-layer-extraction, b905ed5..2dac1a0.

FINAL REVIEW (b905ed5..2dac1a0, 12 commits): Approve with conditions. `bun run verify` run by the reviewer: exit 0, 2314 pass / 0 fail, eslint 0 errors. Spec Slice 1 items 1-12 and 17-23 all implemented and measured; item 24 (the human listening pass) is the only gap.
FINAL REVIEW: all five controller rulings audited as correct. One is under-scoped: the kit-coupling premise behind the widened silent-duplicate predicate holds on the SEQUENCER MENU path (SequencerView.tsx:62-66 and :138-146) but NOT on the vibe/dice path — applyVibeToStore writes `vibe.soundKit`, not `grid.kit`, and a reroll changes only the grid id, so the kit stays the vibe's. Measured: no pool contains such a pair today, so it is a latent hole, not a live bug. Task 6 (the dice) landed after Task 3 (the ruling), so no per-task review could have seen it. Fix is a comment naming the limit.
FINAL REVIEW: CORRECTION TO MY OWN LEDGER — the Task 10 FLAGGED note about `PLAYABLE` had the consequence backwards. I wrote that a 5-row signature would MISS a pair differing only on tom/crash. The opposite is true: a 5-row signature MATCHES such a pair and flags it as a duplicate, so the test goes RED as a false positive rather than failing silently. The risk is a loud wrong failure, not a quiet miss. Left as-is, that note would have sent the next reader to fix the wrong thing.
FINAL REVIEW: must-fix before merge (5, all one-liners): SequencerView.tsx:60-61 and audio/drumGrids.test.ts:50-51 and SequencerView.test.tsx:127 still say "22"/"eight vibe grids" (now 30/7); instantVibesDrumsFixture.ts:5 says it pins "all eight vibes' seven rows" while cyber-edm has eight; instantVibesDrums.test.ts:35-40 widened its key-set check with EXTRA_ROWS but still loops `ROWS` for length/type, so cyber-edm's `bass` row is never checked.
FINAL REVIEW: the remaining 18 deferred minors stay deferred, each with a measured reason. Two minors that a ruling moved into Task 8 were confirmed actually fixed (store.test.ts:309 and :337).
FINAL FIX WAVE: complete (commits 2dac1a0..e3e14c1). Re-review: all 6 findings ADDRESSED, no new breakage, `bun run verify` green. Both source claims in the new kit-coupling comment verified directly (SequencerView.tsx applies grid.kit; vibes.ts:135 applies vibe.soundKit only). Finding 6 confirmed comment-only by filtering the diff to non-comment lines (empty).
LISTENING GATE (spec item 24): the user played the branch and reported the drums are not broken. This was the only spec gap the final review left open. Slice 1 is now complete against the spec.
PART 1 COMPLETE: b905ed5..e3e14c1, 13 commits, verify green (2314 pass / 0 fail, eslint 0 errors).
